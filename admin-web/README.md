# Admin panel

Responsive React/Vite admin panel connected to the Spring Boot API.

```powershell
pnpm install
pnpm dev
```

Vite proxies `/api` to `http://localhost:8080`. Production Docker/Nginx does the same inside the Compose network. Default local login is `admin@desiaahhar.in` / `Admin@123`.
